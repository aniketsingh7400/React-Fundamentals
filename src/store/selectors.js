// Authors selector
export const getAuthors = (state) => state.authorsReducer;

// Courses selector
export const getCourses = (state) => state.coursesReducer;

// User selector
export const getUser = (state) => state.userReducer;
